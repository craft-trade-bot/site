---
import { TELEGRAM_BOT, TELEGRAM_CHANNEL } from '../consts';
---

<div class="container mx-auto p-10">
    <div class="grid lg:grid-cols-2 justify-items-center gap-5">
        <img src="/imgs/home/mockup.webp" alt="mockup" class="anim" />
        <div class="flex flex-col justify-center gap-10 text-center font-bold">
            <h1 class="text-4xl sm:text-6xl lg:text-left text-green-500">CraftTrade</h1>
            <div class="text-xl  sm:text-2xl lg:text-left">Современное приложение для создания торговых ботов на крипторынке</div>
            <a class="p-5 bg-green-500 text-green-950 rounded-4xl text-center text-xl" href={TELEGRAM_BOT}>Начать торговлю</a>
        </div>
    </div>
</div>

<style>
    .anim {
        animation-name: bounce;
        animation-duration: 3s;
        animation-iteration-count: infinite;
    }
    @keyframes bounce {
        0%, 100% {
            transform: translateY(-5%);
            animation-timing-function: cubic-bezier(0.8, 0, 1, 1);
        }
        50% {
            transform: none;
            animation-timing-function: cubic-bezier(0, 0, 0.2, 1);
        }
    }
</style>
